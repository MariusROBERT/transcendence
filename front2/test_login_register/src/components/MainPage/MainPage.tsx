
function MainPage() {
    // if (!authenticated) {
    //     return <Navigate replace to="/login" />;
    //   } else {
    return (
    <div className="App">
      Main Page
    </div>
  );
}

export default MainPage;